<resources>
    <style name="Theme.SophyAdmin" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:colorAccent">@color/sophy_accent</item>
        <item name="android:navigationBarColor">@color/sophy_bg</item>
        <item name="android:statusBarColor">@color/sophy_bg</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <!-- Backward-compatible alias for older manifests -->
    <style name="Theme.SophyReceiver" parent="Theme.SophyAdmin" />
</resources>
